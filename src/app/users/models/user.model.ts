/**
 * User model used for the users module
 *
 * @export
 * @interface User
 */
export interface User {
  id: string;
  firstName: string;
  lastName: string;
  imageUrl: string;
  email: string;
  phone: string;
}
